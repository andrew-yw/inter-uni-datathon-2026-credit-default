# Organizer data (not committed)

Download the competition files from the organizer and place these exact files here:

- `train.csv`
- `test.csv`
- `sample_submission.csv`

The training script validates their SHA-256 fingerprints against `config.json` before use. Do not add the full external UCI dataset or any hidden/source labels to this directory.
